.NET WHOIS Lookup Client
https://github.com/flipbit/whois

This library provides a simple way to access WHOIS information for domain registrations.

Usage:

   var whois = new WhoisLookup().Lookup("microsoft.com");

   Console.WriteLine(whois.ToString());

See the Github project page for more information and sample code.